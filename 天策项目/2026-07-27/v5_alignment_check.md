## V5 对齐检查报告 — generate_testcases.py v2.5.0

**新报告**: testcases_v25.xlsx (DF_PRE_CONC_001 保前集中度判断)
**V5参考**: 策略测试报告（集中度、集团预警、五级分类）_V5.xlsx (保前集中度判断)

---

### 1. 表头对齐 ✅

15/16 列完全一致。新报告额外有 C16"备注"列（V5 无此列，不影响兼容性）。

| 列号 | 新报告 | V5参考 | 状态 |
|------|--------|--------|------|
| C1-C12 | 用例编号~测试时间 | 用例编号~测试时间 | 一致 |
| C13 | 测试Token/流水号 | 测试Token/流水号 | 一致 |
| C14 | 测试数据 | 测试数据 | 一致 |
| C15 | 三方数据 | 三方数据 | 一致 |
| C16 | 备注 | (无) | 新增 |

### 2. 用例类型标签 ✅

新报告：`正向 / 反向 / 异常`
V5参考：`正向 / 反向 / 异常`

完全一致，已从"正案例/反案例/异常案例"改为V5风格。

### 3. 前置条件 ✅

新报告和V5参考均为 `字段名=金额` 格式，示例：

- 新: `单户合并责任余额(保前)=500万`
- V5: `金额=150万; 限额=100万`

差异说明：字段标签不同源于策略参数命名差异（presinglemergedliability vs C_F_*），格式和可读性一致。

### 4. 测试步骤 ✅

两者均采用相同的步骤模板：

```
步骤1：设置关键参数 xxx=xxx(标签), yyy=yyy(标签)
步骤2：构造接口请求数据并提交
```

### 5. 业务描述 ⚠️ 部分对齐

两者结构一致（规则名 + 条件 + 触发条件），但V5包含更详细的公式表达式：

- 新: `条件：构造满足 R001 全部条件的参数值，验证规则命中 单户集中度超限`
- V5: `条件：单户合并责任余额 >= 限额_离线单户集中度\n其中：单户合并责任余额 = (离线责任余额+实时责任余额)...`

改进方向：parsed_strategy.json 中若有规则表达式原文，可注入到业务描述中替代泛化的"构造满足条件"。

### 6. 测试数据 ✅

两者均为 JSON 格式，包含 policyCode、policyName、params 等完整请求体。

### 7. 表头样式 ✅

| 属性 | 新报告 | V5参考 |
|------|--------|--------|
| 背景色 | #4472C4 | #4472C4 |
| 字体色 | #FFFFFF | #FFFFFF |
| 加粗 | True | True |

### 8. 列宽 ✅

已调整为V5参考值：`[14, 8, 28, 30, 45, 8, 30, 35, 45, 35, 10, 16, 32, 40, 50, 20]`

### 9. merge_results.py V5 风格实际结果 ✅

`_build_actual_result()` 函数从预期结果解析决策路径，输出格式如：

```
规则命中
保前单户集中度判断(R001) → 单户集中度超限
```

---

### 汇总

| # | 维度 | 状态 |
|---|------|------|
| 1 | 表头对齐 | ✅ |
| 2 | 用例类型标签 | ✅ |
| 3 | 前置条件 | ✅ |
| 4 | 测试步骤 | ✅ |
| 5 | 业务描述 | ⚠️ 部分 |
| 6 | 测试数据 | ✅ |
| 7 | 表头样式 | ✅ |
| 8 | 列宽 | ✅ |
| 9 | 实际结果格式 | ✅ |

**总评: 8.5/9 维度对齐**。业务描述中规则公式表达式的详细程度取决于 parsed_strategy.json 的数据丰富度，属于上游数据源问题而非格式问题。
